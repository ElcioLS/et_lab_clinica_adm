import 'package:flutter/material.dart';

class EndCheckinPage extends StatelessWidget {
  const EndCheckinPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('End Checkin'),
      ),
      body: Container(),
    );
  }
}
